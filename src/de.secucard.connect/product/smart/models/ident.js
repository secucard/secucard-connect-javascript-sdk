export class Ident {
	
	constructor(type, prefix, name, length, value, valid) {
		this.type = type;
		this.prefix = prefix;
		this.name = name;
		this.length = length;
		this.value = value;
		this.valid = valid;
	}
	
}
